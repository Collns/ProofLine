export function ProofLineLogo({ className }: { className?: string }) {
  return (
    <span className={className ?? 'text-lg font-semibold tracking-tight text-white'}>
      Proof<span className="text-blue-600">Line</span>
    </span>
  );
}
